import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IJobHistoryMySuffix } from '@/shared/model/job-history-my-suffix.model';

const baseApiUrl = 'api/job-histories';

export default class JobHistoryMySuffixService {
  public find(id: number): Promise<IJobHistoryMySuffix> {
    return new Promise<IJobHistoryMySuffix>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IJobHistoryMySuffix): Promise<IJobHistoryMySuffix> {
    return new Promise<IJobHistoryMySuffix>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IJobHistoryMySuffix): Promise<IJobHistoryMySuffix> {
    return new Promise<IJobHistoryMySuffix>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IJobHistoryMySuffix): Promise<IJobHistoryMySuffix> {
    return new Promise<IJobHistoryMySuffix>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
